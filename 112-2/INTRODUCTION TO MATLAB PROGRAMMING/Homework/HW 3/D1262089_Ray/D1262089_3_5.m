P = @(N) 0.15.*50000.*(1+0.15/12).^(12.*N)./(12.*((1+0.15./12).^(12.*N)-1));
p = P(20);
fprintf("%.4f\n", p)
disp('no')
N = 20:1000;
p = P(N);
plot(N, p)
P = @(r) r*50000*(1+r/12)^(12*20)/(12*((1+r/12)^(12*20)-1));
last = 0;
r = 0.15;
p = P(r);
for i = 2:20
    last = p;
    r = r + 0.01;
    p = P(r);    
    disp(p-last)
end