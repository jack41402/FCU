#include <iostream>
#include <stdlib.h>
#include "IQueue_D1271403.h" 

int main(void) {
  IQueue Q; 
  int last, curr; 
  int n; 
  int i, k; 
	
  do {
    do {
      printf(">>>> Enter an integer n (0<=n<=20, stop when n<0): "); 
      scanf("%d", &n); 
      if (n<0) return 0;
	  if(n==0) 
    } while (n>20);
    
  
	 
    printf("-------------------------------\n"); 
  } while (1);
} 

