//Node.cpp
#include "Node.h"
#include<cstddef>
#include<cstdlib>

Node::Node() : elem(0), prev(NULL), next(NULL) //initializing nodes in a linked list data structure
{
    
}

Node::Node(int value) : elem(value), prev(NULL), next(NULL) //initializing nodes in a linked list data structure
{
    
}
